
const KEYS = {
  TAB: 'Tab',
  ENTER: 'Enter',
  ESCAPE: 'Escape',
  UP: 'ArrowUp',
  DOWN: 'ArrowDown'
};

const CUSTOM_MODULES = {
  MENTION: 'mention',
};

const CUSTOM_FORMATS = {
  MENTION_BLOT: 'mentionBlot',
};

export { KEYS, CUSTOM_MODULES, CUSTOM_FORMATS };
